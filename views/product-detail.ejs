<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= product.name %> | DAMORE</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: Arial, sans-serif;
      background: #f8f5ef;
      color: #111;
      line-height: 1.6;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    button,
    input,
    textarea,
    select {
      font: inherit;
    }

    img,
    video {
      max-width: 100%;
      display: block;
    }

    .page {
      max-width: 1240px;
      margin: 0 auto;
      padding: 24px 20px 80px;
    }

    .breadcrumb {
      display: flex;
      align-items: center;
      gap: 8px;
      font-size: 13px;
      color: #777;
      margin-bottom: 20px;
      flex-wrap: wrap;
    }

    .breadcrumb a:hover {
      color: #111;
    }

    .layout {
      display: grid;
      grid-template-columns: 1.08fr 0.92fr;
      gap: 36px;
      align-items: start;
    }

    .gallery-area {
      position: sticky;
      top: 20px;
    }

    .viewer {
      width: 100%;
      border-radius: 20px;
      overflow: hidden;
      background: #efe8dd;
      aspect-ratio: 1 / 1.2;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 14px;
    }

    .viewer img,
    .viewer video {
      width: 100%;
      height: 100%;
      object-fit: cover;
      background: #efe8dd;
    }

    .thumb-list {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
    }

    .thumb-btn {
      width: 74px;
      height: 74px;
      border-radius: 14px;
      overflow: hidden;
      border: 2px solid transparent;
      background: #efe8dd;
      cursor: pointer;
      padding: 0;
      flex-shrink: 0;
    }

    .thumb-btn.active {
      border-color: #111;
    }

    .thumb-btn img,
    .thumb-btn video {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .info-card,
    .section {
      background: #fff;
      border-radius: 24px;
      padding: 28px;
      box-shadow: 0 14px 36px rgba(0, 0, 0, 0.06);
    }

    .category {
      display: inline-block;
      font-size: 12px;
      font-weight: 800;
      color: #9a7d4f;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      margin-bottom: 10px;
    }

    .title {
      font-size: 32px;
      font-weight: 900;
      line-height: 1.25;
      margin-bottom: 10px;
      word-break: keep-all;
    }

    .summary {
      font-size: 14px;
      color: #666;
      margin-bottom: 14px;
      white-space: pre-line;
      word-break: keep-all;
    }

    .tag-row {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-bottom: 18px;
    }

    .chip {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-height: 32px;
      padding: 0 12px;
      border-radius: 999px;
      font-size: 12px;
      font-weight: 800;
    }

    .chip-dark {
      background: #111;
      color: #fff;
    }

    .chip-soft {
      background: #f4ede2;
      color: #8b6230;
    }

    .price-box {
      display: flex;
      align-items: end;
      gap: 10px;
      flex-wrap: wrap;
      margin-bottom: 18px;
    }

    .price {
      font-size: 32px;
      font-weight: 900;
      line-height: 1;
    }

    .old-price {
      font-size: 16px;
      color: #999;
      text-decoration: line-through;
      margin-bottom: 3px;
    }

    .discount {
      font-size: 15px;
      font-weight: 800;
      color: #c0392b;
      margin-bottom: 2px;
    }

    .stock {
      font-size: 14px;
      font-weight: 800;
      margin-bottom: 20px;
      color: #111;
    }

    .stock.soldout {
      color: #c0392b;
    }

    .desc {
      font-size: 14px;
      color: #555;
      white-space: pre-line;
      word-break: keep-all;
      margin-bottom: 22px;
    }

    .divider {
      height: 1px;
      background: #eee6da;
      margin: 22px 0;
    }

    .form-group {
      margin-bottom: 18px;
    }

    .label {
      display: block;
      font-size: 14px;
      font-weight: 800;
      margin-bottom: 10px;
    }

    .option-wrap {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
    }

    .option-btn,
    .color-btn {
      min-width: 74px;
      min-height: 44px;
      padding: 10px 16px;
      border-radius: 999px;
      border: 1px solid #ddd6ca;
      background: #fff;
      cursor: pointer;
      font-weight: 800;
      color: #111;
    }

    .option-btn.active,
    .color-btn.active {
      background: #111;
      color: #fff;
      border-color: #111;
    }

    .option-btn.soldout,
    .color-btn.soldout {
      opacity: 0.4;
      text-decoration: line-through;
      cursor: not-allowed;
    }

    .qty-row {
      display: flex;
      align-items: center;
      gap: 14px;
      flex-wrap: wrap;
    }

    .qty-box {
      display: inline-flex;
      align-items: center;
      border: 1px solid #ddd6ca;
      border-radius: 999px;
      overflow: hidden;
      background: #fff;
    }

    .qty-btn {
      width: 42px;
      height: 42px;
      border: 0;
      background: #fff;
      cursor: pointer;
      font-size: 18px;
      font-weight: 900;
    }

    .qty-input {
      width: 60px;
      height: 42px;
      border: 0;
      text-align: center;
      outline: none;
      font-weight: 800;
    }

    .error-text {
      min-height: 18px;
      margin-top: 8px;
      font-size: 13px;
      color: #c0392b;
      font-weight: 700;
    }

    .add-on-list {
      display: grid;
      gap: 10px;
    }

    .add-on-item {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      border: 1px solid #e7dfd3;
      border-radius: 16px;
      padding: 12px;
      background: #fcfaf7;
    }

    .add-on-left {
      display: flex;
      align-items: center;
      gap: 12px;
      min-width: 0;
    }

    .add-on-check {
      width: 18px;
      height: 18px;
      flex-shrink: 0;
      cursor: pointer;
    }

    .add-on-thumb {
      width: 56px;
      height: 56px;
      border-radius: 12px;
      object-fit: cover;
      background: #efe8dd;
      flex-shrink: 0;
    }

    .add-on-info {
      min-width: 0;
    }

    .add-on-name {
      font-size: 14px;
      font-weight: 800;
      color: #111;
      word-break: keep-all;
    }

    .add-on-price {
      font-size: 13px;
      color: #666;
      margin-top: 2px;
    }

    .total-box {
      margin-top: 24px;
      background: #f4ede2;
      border-radius: 18px;
      padding: 18px 20px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
    }

    .total-label {
      font-size: 14px;
      color: #666;
      font-weight: 700;
    }

    .total-price {
      font-size: 28px;
      font-weight: 900;
    }

    .action-row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 12px;
      margin-top: 16px;
    }

    .action-btn {
      height: 54px;
      border: 0;
      border-radius: 14px;
      cursor: pointer;
      font-size: 15px;
      font-weight: 900;
    }

    .action-btn.light {
      background: #e8dfd2;
      color: #111;
    }

    .action-btn.dark {
      background: #111;
      color: #fff;
    }

    .action-btn:disabled {
      background: #d9d2c8;
      color: #7a7368;
      cursor: not-allowed;
    }

    .section {
      margin-top: 34px;
    }

    .section-title {
      font-size: 24px;
      font-weight: 900;
      margin-bottom: 18px;
    }

    .detail-images {
      display: grid;
      gap: 18px;
    }

    .detail-images img {
      width: 100%;
      border-radius: 18px;
    }

    .guide-block + .guide-block {
      margin-top: 28px;
    }

    .guide-label {
      font-size: 15px;
      font-weight: 900;
      margin-bottom: 10px;
    }

    .guide-text {
      font-size: 14px;
      color: #555;
      white-space: pre-line;
      word-break: keep-all;
    }

    .empty {
      font-size: 14px;
      color: #777;
    }

    .review-summary {
      display: grid;
      grid-template-columns: 220px 1fr;
      gap: 24px;
      margin-bottom: 24px;
      align-items: center;
    }

    .review-score-box {
      background: #faf7f2;
      border-radius: 20px;
      padding: 22px;
      text-align: center;
    }

    .review-score {
      font-size: 42px;
      font-weight: 900;
      line-height: 1;
      margin-bottom: 6px;
    }

    .review-count {
      font-size: 14px;
      color: #666;
      font-weight: 700;
    }

    .star-row {
      display: flex;
      gap: 2px;
      justify-content: center;
      margin-bottom: 8px;
      color: #c8a96b;
      font-size: 18px;
    }

    .review-list,
    .inquiry-list {
      display: grid;
      gap: 14px;
    }

    .review-item,
    .inquiry-item {
      border: 1px solid #ece4d8;
      border-radius: 18px;
      padding: 18px;
      background: #fcfaf7;
    }

    .review-head,
    .inquiry-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      flex-wrap: wrap;
      margin-bottom: 8px;
    }

    .review-author,
    .inquiry-author {
      font-weight: 800;
      font-size: 14px;
    }

    .review-date,
    .inquiry-date {
      font-size: 12px;
      color: #888;
    }

    .review-body,
    .inquiry-body {
      font-size: 14px;
      color: #555;
      white-space: pre-line;
      word-break: keep-all;
    }

    .tab-nav {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
      margin-bottom: 20px;
    }

    .tab-btn {
      min-height: 42px;
      padding: 0 16px;
      border-radius: 999px;
      border: 1px solid #ddd6ca;
      background: #fff;
      cursor: pointer;
      font-weight: 800;
    }

    .tab-btn.active {
      background: #111;
      color: #fff;
      border-color: #111;
    }

    .tab-panel {
      display: none;
    }

    .tab-panel.active {
      display: block;
    }

    .inquiry-form {
      display: grid;
      gap: 12px;
      margin-top: 20px;
      padding-top: 20px;
      border-top: 1px solid #eee6da;
    }

    .text-input,
    .text-area {
      width: 100%;
      border: 1px solid #ddd6ca;
      border-radius: 14px;
      background: #fff;
      padding: 14px 16px;
      outline: none;
    }

    .text-area {
      min-height: 120px;
      resize: vertical;
    }

    .submit-btn {
      height: 48px;
      border: 0;
      border-radius: 14px;
      background: #111;
      color: #fff;
      font-weight: 900;
      cursor: pointer;
    }

    .recommend-grid {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 18px;
    }

    .recommend-card {
      display: block;
      border-radius: 18px;
      overflow: hidden;
      background: #fcfaf7;
      border: 1px solid #ece4d8;
    }

    .recommend-thumb {
      width: 100%;
      aspect-ratio: 1 / 1.18;
      object-fit: cover;
      background: #efe8dd;
    }

    .recommend-info {
      padding: 14px;
    }

    .recommend-name {
      font-size: 14px;
      font-weight: 800;
      line-height: 1.45;
      min-height: 40px;
      word-break: keep-all;
      margin-bottom: 6px;
    }

    .recommend-price {
      font-size: 15px;
      font-weight: 900;
    }

    @media (max-width: 980px) {
      .layout {
        grid-template-columns: 1fr;
      }

      .gallery-area {
        position: static;
      }

      .review-summary {
        grid-template-columns: 1fr;
      }

      .recommend-grid {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }
    }

    @media (max-width: 640px) {
      .page {
        padding: 16px 14px 60px;
      }

      .info-card,
      .section {
        padding: 20px;
      }

      .title {
        font-size: 26px;
      }

      .price {
        font-size: 28px;
      }

      .total-price {
        font-size: 24px;
      }

      .action-row {
        grid-template-columns: 1fr;
      }

      .recommend-grid {
        grid-template-columns: 1fr 1fr;
        gap: 12px;
      }
    }
  </style>
</head>
<body>
  <%
    const safeSubImages = Array.isArray(product.subImages) ? product.subImages.filter(Boolean) : [];
    const safeDetailImages = Array.isArray(product.detailImages) ? product.detailImages.filter(Boolean) : [];
    const safeDesc = product.desc || product.description || '';
    const safeSummary = product.summary || '';
    const isSoldOut = product.status === 'soldout' || Number(product.stock || 0) <= 0;
    const safeAddOnProducts = Array.isArray(addOnProducts) ? addOnProducts : [];
    const safeOptionGroups = Array.isArray(product.optionGroups) ? product.optionGroups.filter(Boolean) : [];

    const legacyOptions = Array.isArray(product.options)
      ? product.options
      : (typeof product.options === 'string'
          ? product.options.split(',').map(v => v.trim()).filter(Boolean)
          : []);

    const legacyColors = Array.isArray(product.colors)
      ? product.colors
      : (typeof product.colors === 'string'
          ? product.colors.split(',').map(v => v.trim()).filter(Boolean)
          : []);

    let firstGroup = null;
    let secondGroup = null;

    if (safeOptionGroups.length >= 1) firstGroup = safeOptionGroups[0];
    if (safeOptionGroups.length >= 2) secondGroup = safeOptionGroups[1];

    if (!firstGroup && legacyOptions.length > 0) {
      firstGroup = {
        name: '옵션',
        values: legacyOptions.map((value) => ({ value, isSoldOut: false }))
      };
    }

    if (!secondGroup && legacyColors.length > 0) {
      secondGroup = {
        name: '컬러',
        values: legacyColors.map((value) => ({ value, isSoldOut: false }))
      };
    }

    const galleryItems = [];
    if (product.image) galleryItems.push({ type: 'image', src: product.image });
    if (product.video) galleryItems.push({ type: 'video', src: product.video });

    safeSubImages.forEach((img) => {
      if (!galleryItems.find((item) => item.src === img)) {
        galleryItems.push({ type: 'image', src: img });
      }
    });

    const discountRate =
      Number(product.oldPrice || 0) > Number(product.price || 0)
        ? Math.round((1 - Number(product.price || 0) / Number(product.oldPrice || 0)) * 100)
        : 0;

    const fakeReviews = [
      {
        author: 'damore***',
        date: '2026-04-18',
        rating: 5,
        body: '핏이 진짜 예쁘고 원단이 생각보다 더 좋았어요. 데일리로 입기 좋습니다.'
      },
      {
        author: 'style***',
        date: '2026-04-16',
        rating: 4,
        body: '색감이 화면이랑 거의 비슷하고 사이즈도 정사이즈 느낌입니다.'
      }
    ];

    const fakeInquiries = [
      {
        author: 'guest***',
        date: '2026-04-20',
        title: '배송 문의',
        body: '오늘 주문하면 언제 출고되나요?'
      },
      {
        author: 'look***',
        date: '2026-04-19',
        title: '사이즈 문의',
        body: '평소 100 입는데 이 상품은 어떤 사이즈 추천인가요?'
      }
    ];

    const recommendProducts = safeAddOnProducts.slice(0, 4);
  %>

  <div
    id="productData"
    data-price="<%= Number(product.price || 0) %>"
    data-stock="<%= Number(product.stock || 0) %>"
    data-has-first-group="<%= firstGroup && Array.isArray(firstGroup.values) && firstGroup.values.length > 0 ? 'true' : 'false' %>"
    data-has-second-group="<%= secondGroup && Array.isArray(secondGroup.values) && secondGroup.values.length > 0 ? 'true' : 'false' %>"
  ></div>

  <div class="page">
    <div class="breadcrumb">
      <a href="/">홈</a>
      <% if (product.category) { %>
        <span>/</span>
        <a href="/category/<%= product.category %>"><%= product.category %></a>
      <% } %>
      <span>/</span>
      <span><%= product.name %></span>
    </div>

    <div class="layout">
      <div class="gallery-area">
        <div class="viewer" id="galleryStage">
          <% if (galleryItems.length > 0) { %>
            <% if (galleryItems[0].type === 'video') { %>
              <video src="<%= galleryItems[0].src %>" controls playsinline preload="metadata"></video>
            <% } else { %>
              <img src="<%= galleryItems[0].src %>" alt="<%= product.name %>">
            <% } %>
          <% } %>
        </div>

        <% if (galleryItems.length > 1) { %>
          <div class="thumb-list" id="thumbs">
            <% galleryItems.forEach((item, index) => { %>
              <button
                type="button"
                class="thumb-btn <%= index === 0 ? 'active' : '' %>"
                data-type="<%= item.type %>"
                data-src="<%= item.src %>"
              >
                <% if (item.type === 'video') { %>
                  <video src="<%= item.src %>" muted playsinline preload="metadata"></video>
                <% } else { %>
                  <img src="<%= item.src %>" alt="썸네일">
                <% } %>
              </button>
            <% }) %>
          </div>
        <% } %>
      </div>

      <div class="info-card">
        <% if (product.category) { %>
          <div class="category"><%= product.category %></div>
        <% } %>

        <div class="title"><%= product.name %></div>

        <% if (safeSummary) { %>
          <div class="summary"><%= safeSummary %></div>
        <% } %>

        <div class="tag-row">
          <% if (product.tag) { %>
            <div class="chip chip-dark"><%= product.tag %></div>
          <% } %>

          <% if (product.couponDisplay && product.couponDisplay.enabled && product.couponDisplay.label) { %>
            <div class="chip chip-soft"><%= product.couponDisplay.label %></div>
          <% } %>
        </div>

        <div class="price-box">
          <% if (discountRate > 0) { %>
            <div class="discount"><%= discountRate %>%</div>
          <% } %>
          <div class="price"><%= Number(product.price || 0).toLocaleString() %>원</div>
          <% if (Number(product.oldPrice || 0) > 0) { %>
            <div class="old-price"><%= Number(product.oldPrice || 0).toLocaleString() %>원</div>
          <% } %>
        </div>

        <div class="stock <%= isSoldOut ? 'soldout' : '' %>">
          <% if (isSoldOut) { %>
            품절된 상품입니다
          <% } else { %>
            재고 <%= Number(product.stock || 0) %>개 · <%= product.shippingFeeText || '무료배송' %>
          <% } %>
        </div>

        <% if (safeDesc) { %>
          <div class="desc"><%= safeDesc %></div>
        <% } %>

        <div class="divider"></div>

        <form id="productForm" method="POST">
          <input type="hidden" name="productId" value="<%= product._id %>">
          <input type="hidden" name="selectedOption" id="selectedOptionInput" value="">
          <input type="hidden" name="option" id="legacyOptionInput" value="">
          <input type="hidden" name="selectedColor" id="selectedColorInput" value="">
          <input type="hidden" name="quantity" id="quantityHidden" value="1">
          <input type="hidden" name="selectedAddOnIds" id="selectedAddOnIdsInput" value="">
          <input type="hidden" name="selectedAddOnNames" id="selectedAddOnNamesInput" value="">
          <input type="hidden" name="selectedAddOnTotal" id="selectedAddOnTotalInput" value="0">

          <% if (firstGroup && Array.isArray(firstGroup.values) && firstGroup.values.length > 0) { %>
            <div class="form-group">
              <label class="label"><%= firstGroup.name || '옵션 선택' %></label>
              <div class="option-wrap" id="optionWrap">
                <% firstGroup.values.forEach((item) => { %>
                  <button
                    type="button"
                    class="option-btn <%= item.isSoldOut ? 'soldout' : '' %>"
                    data-option="<%= item.value %>"
                    data-soldout="<%= item.isSoldOut ? 'true' : 'false' %>"
                  >
                    <%= item.value %>
                  </button>
                <% }) %>
              </div>
              <div class="error-text" id="optionError"></div>
            </div>
          <% } %>

          <% if (secondGroup && Array.isArray(secondGroup.values) && secondGroup.values.length > 0) { %>
            <div class="form-group">
              <label class="label"><%= secondGroup.name || '컬러 선택' %></label>
              <div class="option-wrap" id="colorWrap">
                <% secondGroup.values.forEach((item) => { %>
                  <button
                    type="button"
                    class="color-btn <%= item.isSoldOut ? 'soldout' : '' %>"
                    data-color="<%= item.value %>"
                    data-soldout="<%= item.isSoldOut ? 'true' : 'false' %>"
                  >
                    <%= item.value %>
                  </button>
                <% }) %>
              </div>
              <div class="error-text" id="colorError"></div>
            </div>
          <% } %>

          <div class="form-group">
            <label class="label">수량</label>
            <div class="qty-row">
              <div class="qty-box">
                <button type="button" class="qty-btn" id="minusBtn">-</button>
                <input
                  type="number"
                  class="qty-input"
                  id="qtyInput"
                  value="1"
                  min="1"
                  max="<%= Math.max(Number(product.stock || 1), 1) %>"
                />
                <button type="button" class="qty-btn" id="plusBtn">+</button>
              </div>
            </div>
          </div>

          <% if (safeAddOnProducts.length > 0) { %>
            <div class="form-group">
              <label class="label">추가상품</label>
              <div class="add-on-list" id="addOnList">
                <% safeAddOnProducts.forEach((item) => { %>
                  <label class="add-on-item">
                    <div class="add-on-left">
                      <input
                        type="checkbox"
                        class="add-on-check"
                        data-id="<%= item._id %>"
                        data-name="<%= item.name %>"
                        data-price="<%= Number(item.price || 0) %>"
                      />
                      <% if (item.image) { %>
                        <img src="<%= item.image %>" alt="<%= item.name %>" class="add-on-thumb">
                      <% } else { %>
                        <div class="add-on-thumb"></div>
                      <% } %>
                      <div class="add-on-info">
                        <div class="add-on-name"><%= item.name %></div>
                        <div class="add-on-price"><%= Number(item.price || 0).toLocaleString() %>원</div>
                      </div>
                    </div>
                  </label>
                <% }) %>
              </div>
            </div>
          <% } %>

          <div class="total-box">
            <div class="total-label">총 상품금액</div>
            <div class="total-price" id="totalPrice"><%= Number(product.price || 0).toLocaleString() %>원</div>
          </div>

          <div class="action-row">
            <button
              type="button"
              class="action-btn light"
              id="cartBtn"
              <%= isSoldOut ? 'disabled' : '' %>
            >
              장바구니 담기
            </button>

            <button
              type="button"
              class="action-btn dark"
              id="buyBtn"
              <%= isSoldOut ? 'disabled' : '' %>
            >
              바로구매
            </button>
          </div>
        </form>
      </div>
    </div>

    <div class="section">
      <div class="tab-nav">
        <button type="button" class="tab-btn active" data-tab="detail">상세정보</button>
        <button type="button" class="tab-btn" data-tab="review">리뷰</button>
        <button type="button" class="tab-btn" data-tab="inquiry">문의</button>
      </div>

      <div class="tab-panel active" data-panel="detail">
        <div class="section-title">상품 상세정보</div>

        <% if (safeDetailImages.length > 0) { %>
          <div class="detail-images">
            <% safeDetailImages.forEach((img, index) => { %>
              <img src="<%= img %>" alt="상세 이미지 <%= index + 1 %>">
            <% }) %>
          </div>
        <% } else if (safeDesc) { %>
          <div class="desc"><%= safeDesc %></div>
        <% } else { %>
          <div class="empty">등록된 상세 정보가 없습니다.</div>
        <% } %>
      </div>

      <div class="tab-panel" data-panel="review">
        <div class="section-title">상품 리뷰</div>

        <div class="review-summary">
          <div class="review-score-box">
            <div class="review-score">4.8</div>
            <div class="star-row">★★★★★</div>
            <div class="review-count">총 <%= Number(product.reviewCount || fakeReviews.length) %>개 리뷰</div>
          </div>

          <div class="desc" style="margin:0;">
            구매 고객들이 만족한 상품이에요.  
            원단, 핏, 착용감 관련 반응이 좋은 편입니다.
          </div>
        </div>

        <div class="review-list">
          <% fakeReviews.forEach((review) => { %>
            <div class="review-item">
              <div class="review-head">
                <div class="review-author"><%= review.author %></div>
                <div class="review-date"><%= review.date %></div>
              </div>
              <div class="star-row" style="justify-content:flex-start; margin-bottom:10px;">
                <% for (let i = 0; i < review.rating; i++) { %>★<% } %>
              </div>
              <div class="review-body"><%= review.body %></div>
            </div>
          <% }) %>
        </div>
      </div>

      <div class="tab-panel" data-panel="inquiry">
        <div class="section-title">상품 문의</div>

        <div class="inquiry-list">
          <% fakeInquiries.forEach((item) => { %>
            <div class="inquiry-item">
              <div class="inquiry-head">
                <div class="inquiry-author"><%= item.author %> · <%= item.title %></div>
                <div class="inquiry-date"><%= item.date %></div>
              </div>
              <div class="inquiry-body"><%= item.body %></div>
            </div>
          <% }) %>
        </div>

        <form class="inquiry-form" onsubmit="event.preventDefault(); alert('문의 등록 기능은 다음 단계에서 연결하면 됨');">
          <input class="text-input" type="text" placeholder="문의 제목">
          <textarea class="text-area" placeholder="문의 내용을 입력하세요"></textarea>
          <button class="submit-btn" type="submit">문의 등록</button>
        </form>
      </div>
    </div>

    <% if (
      (product.guide && product.guide.sizeGuideImage) ||
      (product.guide && product.guide.washImage) ||
      (product.guide && product.guide.shippingExchangeReturn) ||
      (product.guide && product.guide.copyrightNotice)
    ) { %>
      <div class="section">
        <div class="section-title">구매 안내</div>

        <% if (product.guide && product.guide.sizeGuideImage) { %>
          <div class="guide-block">
            <div class="guide-label">사이즈 가이드</div>
            <img src="<%= product.guide.sizeGuideImage %>" alt="사이즈 가이드">
          </div>
        <% } %>

        <% if (product.guide && product.guide.washImage) { %>
          <div class="guide-block">
            <div class="guide-label">세탁 안내</div>
            <img src="<%= product.guide.washImage %>" alt="세탁 안내">
          </div>
        <% } %>

        <% if (product.guide && product.guide.shippingExchangeReturn) { %>
          <div class="guide-block">
            <div class="guide-label">배송 / 교환 / 반품 안내</div>
            <div class="guide-text"><%= product.guide.shippingExchangeReturn %></div>
          </div>
        <% } %>

        <% if (product.guide && product.guide.copyrightNotice) { %>
          <div class="guide-block">
            <div class="guide-label">저작권 안내</div>
            <div class="guide-text"><%= product.guide.copyrightNotice %></div>
          </div>
        <% } %>
      </div>
    <% } %>

    <% if (recommendProducts.length > 0) { %>
      <div class="section">
        <div class="section-title">추천 상품</div>
        <div class="recommend-grid">
          <% recommendProducts.forEach((item) => { %>
            <a class="recommend-card" href="/products/<%= item._id %>">
              <% if (item.image) { %>
                <img class="recommend-thumb" src="<%= item.image %>" alt="<%= item.name %>">
              <% } else { %>
                <div class="recommend-thumb"></div>
              <% } %>
              <div class="recommend-info">
                <div class="recommend-name"><%= item.name %></div>
                <div class="recommend-price"><%= Number(item.price || 0).toLocaleString() %>원</div>
              </div>
            </a>
          <% }) %>
        </div>
      </div>
    <% } %>
  </div>

  <script>
    const productDataEl = document.getElementById('productData');

    const price = Number(productDataEl?.dataset.price || 0);
    const stock = Number(productDataEl?.dataset.stock || 0);
    const hasFirstGroup = productDataEl?.dataset.hasFirstGroup === 'true';
    const hasSecondGroup = productDataEl?.dataset.hasSecondGroup === 'true';

    const optionWrap = document.getElementById('optionWrap');
    const optionButtons = optionWrap ? optionWrap.querySelectorAll('.option-btn') : [];
    const selectedOptionInput = document.getElementById('selectedOptionInput');
    const legacyOptionInput = document.getElementById('legacyOptionInput');
    const optionError = document.getElementById('optionError');

    const colorWrap = document.getElementById('colorWrap');
    const colorButtons = colorWrap ? colorWrap.querySelectorAll('.color-btn') : [];
    const selectedColorInput = document.getElementById('selectedColorInput');
    const colorError = document.getElementById('colorError');

    const addOnList = document.getElementById('addOnList');
    const addOnChecks = addOnList ? addOnList.querySelectorAll('.add-on-check') : [];
    const selectedAddOnIdsInput = document.getElementById('selectedAddOnIdsInput');
    const selectedAddOnNamesInput = document.getElementById('selectedAddOnNamesInput');
    const selectedAddOnTotalInput = document.getElementById('selectedAddOnTotalInput');

    const qtyInput = document.getElementById('qtyInput');
    const quantityHidden = document.getElementById('quantityHidden');
    const minusBtn = document.getElementById('minusBtn');
    const plusBtn = document.getElementById('plusBtn');
    const totalPrice = document.getElementById('totalPrice');

    const productForm = document.getElementById('productForm');
    const cartBtn = document.getElementById('cartBtn');
    const buyBtn = document.getElementById('buyBtn');

    const galleryStage = document.getElementById('galleryStage');
    const thumbs = document.getElementById('thumbs');

    function getSelectedAddOnTotal() {
      let sum = 0;
      addOnChecks.forEach((check) => {
        if (check.checked) {
          sum += Number(check.dataset.price || 0);
        }
      });
      return sum;
    }

    function syncAddOnInputs() {
      const ids = [];
      const names = [];
      let total = 0;

      addOnChecks.forEach((check) => {
        if (check.checked) {
          ids.push(check.dataset.id || '');
          names.push(check.dataset.name || '');
          total += Number(check.dataset.price || 0);
        }
      });

      if (selectedAddOnIdsInput) selectedAddOnIdsInput.value = ids.join(',');
      if (selectedAddOnNamesInput) selectedAddOnNamesInput.value = names.join(',');
      if (selectedAddOnTotalInput) selectedAddOnTotalInput.value = String(total);
    }

    function updateTotal() {
      let qty = Number(qtyInput?.value || 1);

      if (qty < 1) qty = 1;
      if (stock > 0 && qty > stock) qty = stock;

      if (qtyInput) qtyInput.value = qty;
      if (quantityHidden) quantityHidden.value = qty;

      const addOnTotal = getSelectedAddOnTotal();
      const finalTotal = (price * qty) + addOnTotal;

      if (totalPrice) {
        totalPrice.textContent = finalTotal.toLocaleString() + '원';
      }

      syncAddOnInputs();
    }

    if (minusBtn) {
      minusBtn.addEventListener('click', function () {
        qtyInput.value = Math.max(Number(qtyInput.value || 1) - 1, 1);
        updateTotal();
      });
    }

    if (plusBtn) {
      plusBtn.addEventListener('click', function () {
        const next = Number(qtyInput.value || 1) + 1;
        qtyInput.value = stock > 0 ? Math.min(next, stock) : next;
        updateTotal();
      });
    }

    if (qtyInput) {
      qtyInput.addEventListener('input', updateTotal);
    }

    optionButtons.forEach((button) => {
      button.addEventListener('click', function () {
        if (button.dataset.soldout === 'true') return;

        optionButtons.forEach((btn) => btn.classList.remove('active'));
        button.classList.add('active');

        const value = button.dataset.option || '';
        if (selectedOptionInput) selectedOptionInput.value = value;
        if (legacyOptionInput) legacyOptionInput.value = value;
        if (optionError) optionError.textContent = '';
      });
    });

    colorButtons.forEach((button) => {
      button.addEventListener('click', function () {
        if (button.dataset.soldout === 'true') return;

        colorButtons.forEach((btn) => btn.classList.remove('active'));
        button.classList.add('active');

        const value = button.dataset.color || '';
        if (selectedColorInput) selectedColorInput.value = value;
        if (colorError) colorError.textContent = '';
      });
    });

    addOnChecks.forEach((check) => {
      check.addEventListener('change', function () {
        updateTotal();
      });
    });

    function validateForm() {
      if (hasFirstGroup && selectedOptionInput && !selectedOptionInput.value) {
        if (optionError) optionError.textContent = '옵션을 선택해주세요.';
        return false;
      }

      if (hasSecondGroup && selectedColorInput && !selectedColorInput.value) {
        if (colorError) colorError.textContent = '컬러를 선택해주세요.';
        return false;
      }

      return true;
    }

    if (cartBtn) {
      cartBtn.addEventListener('click', function () {
        if (!validateForm()) return;
        productForm.action = '/cart/add';
        productForm.submit();
      });
    }

    if (buyBtn) {
      buyBtn.addEventListener('click', function () {
        if (!validateForm()) return;
        productForm.action = '/buy-now';
        productForm.submit();
      });
    }

    if (thumbs && galleryStage) {
      thumbs.addEventListener('click', function (e) {
        const thumb = e.target.closest('.thumb-btn');
        if (!thumb) return;

        const type = thumb.dataset.type;
        const src = thumb.dataset.src;

        if (type === 'video') {
          galleryStage.innerHTML = `<video src="${src}" controls playsinline preload="metadata"></video>`;
        } else {
          galleryStage.innerHTML = `<img src="${src}" alt="상품 이미지">`;
        }

        thumbs.querySelectorAll('.thumb-btn').forEach((item) => {
          item.classList.remove('active');
        });
        thumb.classList.add('active');
      });
    }

    const tabButtons = document.querySelectorAll('.tab-btn');
    const tabPanels = document.querySelectorAll('.tab-panel');

    tabButtons.forEach((button) => {
      button.addEventListener('click', function () {
        const target = button.dataset.tab;

        tabButtons.forEach((btn) => btn.classList.remove('active'));
        tabPanels.forEach((panel) => panel.classList.remove('active'));

        button.classList.add('active');
        const panel = document.querySelector(`[data-panel="${target}"]`);
        if (panel) panel.classList.add('active');
      });
    });

    updateTotal();
  </script>
</body>
</html>